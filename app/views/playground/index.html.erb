<% content_for :page_title, "Component Playground" %>

<div class="max-w-7xl mx-auto">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">AppStack → Tailwind Conversion</h1>
    <p class="text-gray-600">Component gallery and testing ground</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% @components.each do |component| %>
      <%= link_to playground_path(component), 
                  class: "block p-6 bg-white border border-gray-200 rounded-lg hover:border-blue-500 hover:shadow-md transition-all" do %>
        <h3 class="text-lg font-semibold text-gray-900 mb-2 capitalize">
          <%= component.titleize %>
        </h3>
        <p class="text-sm text-gray-600">
          View <%= component %> components
        </p>
      <% end %>
    <% end %>
  </div>
  
  <div class="mt-12 p-6 bg-blue-50 border border-blue-200 rounded-lg">
    <h2 class="text-lg font-semibold text-blue-900 mb-2">Quick Start</h2>
    <ol class="list-decimal list-inside text-blue-800 space-y-1">
      <li>Original AppStack files are in <code class="text-sm bg-blue-100 px-1 rounded">vendor/appstack/</code></li>
      <li>New Tailwind components go in <code class="text-sm bg-blue-100 px-1 rounded">app/views/components/</code></li>
      <li>Track progress in <code class="text-sm bg-blue-100 px-1 rounded">docs/COMPONENTS.md</code></li>
    </ol>
  </div>
</div>
